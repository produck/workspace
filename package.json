{
  "name": "@produck/workspace-workspace",
  "private": true,
  "description": "",
  "scripts": {
    "deps:install": "npm install && lerna bootstrap",
    "deps:clean": "lerna clean -y"
  },
  "devDependencies": {
    "eslint": "^8.28.0",
    "lerna": "^6.0.3"
  },
  "workspaces": [
    "packages/*"
  ]
}
